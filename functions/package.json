{
  "name": "reboost-hub-functions",
  "version": "1.0.0",
  "description": "ReBoost Hub Firebase Cloud Functions",
  "engines": { "node": "22" },
  "main": "src/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project reboost-hub",
    "lint": "eslint src/"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-google": "^0.14.0"
  },
  "private": true
}
